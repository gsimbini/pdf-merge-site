'use client';

import { useState } from 'react';

export default function ClientToolWrapper({ children }) {
  return children;
}

// Or if you want to manage file state here, but simplest is just wrapper