import ToolPage from '../components/ToolPage';

export default function ExcelToPdfPage() {
  return (
    <ToolPage
      title="Excel to PDF"
      slug="excel-to-pdf"
      shortDescription="Convert Excel spreadsheets into PDF documents."
      longDescription="Useful for sharing reports that must look consistent on any device."
    />
  );
}
